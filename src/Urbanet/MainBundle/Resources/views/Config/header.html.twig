{% if is_granted("IS_AUTHENTICATED_FULLY") %}
    <div class="navbar navbar-fixed-top">
            <div class="navbar-inner ">
                <a class="btn btn-navbar collapsed" data-toggle="collapse" data-target=".navbar-responsive-collapse">
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                          </a>
                <a class="brand" href="{{ path('urbanet_main_homepage') }}">
                    {% image '%kernel.root_dir%/../src/Urbanet/MainBundle/Resources/public/images/FBLogo.png' %}
                        <img src="{{ asset_url }}" alt="Urbanetwork logo" class="img-circle" /></a>  
                    {% endimage %}
                </a>

                <div class="nav-collapse collapse navbar-responsive-collapse" />
                <ul class="nav pull-left">
                    <li class="active">
                        <a href="{{ path('urbanet_main_homepage') }}">Acceuil</a>
                    </li>
                </ul>

                <ul class="nav pull-right">
                    <form class="navbar-search" action="{{ path('urbanet_main_recherche') }}">
                        <input class="search-query span4" type="text" placeholder="Rechercher Annonce"></input>
                    </form>
                    <li class="divider-vertical"></li>
                    <li><a href="{{ path('fos_user_security_login') }}"><i class="icon-user"></i>{{app.user.username}}</a></li>
                    <li><a href="{{path('fos_user_security_logout')}}">Logout</a></li>
                </ul>
                </div>
            </div>
         </div>

    {% else %}
        <div class="navbar navbar-fixed-top">
            <div class="navbar-inner ">
                <a class="btn btn-navbar collapsed" data-toggle="collapse" data-target=".navbar-responsive-collapse">
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                          </a>
                <a class="brand" href="{{ path('urbanet_main_homepage') }}">
                    {% image '%kernel.root_dir%/../src/Urbanet/MainBundle/Resources/public/images/FBLogo.png' %}
                        <img src="{{ asset_url }}" alt="Urbanetwork logo" class="img-circle" /></a>  
                    {% endimage %}
                </a>

                <div class="nav-collapse collapse navbar-responsive-collapse" />
                <ul class="nav pull-left">
                    <li class="active">
                        <a href="{{ path('urbanet_main_homepage') }}">Acceuil</a>
                    </li>
                    <li><a href="{{ path('urbanet_user_nosartistes') }}">Découvrez nos artistes</a></li>
                    <li><a href="{{ path('urbanet_annonce_nosannonceurs') }}">Découvrez nos annonceurs</a></li> 
                </ul>

                <ul class="nav pull-right">
                    <form class="navbar-search" action="{{ path('urbanet_main_recherche') }}">
                        <input class="search-query span4" type="text" placeholder="Rechercher Annonce"></input>
                    </form>
                    <li class="divider-vertical"></li>
                    <li><a href="{{ path('fos_user_security_login') }}"><i class="icon-user"></i>Login</a></li>
                </ul>
                </div>
            </div>
         </div>
        
    {% endif %}